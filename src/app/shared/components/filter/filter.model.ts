export interface filterData{
  key: string;
  value: string | number;
}

export interface filterType {
  type: string[]
  adType: string[]
  country: string[]
  city: string[]
  floor: number[]
  rooms: number[]
}